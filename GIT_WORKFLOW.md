# Git Workflow - Пошаговий гайд

## 📋 Правила роботи з гілками та Pull Request

---

## 🚀 КРОК 1: Створення нової feature-гілки

### 1.1. Перевірка поточного стану
```bash
git status
```
**Переконайтеся, що:**
- Ви на гілці `main`
- Немає незакомічених змін (або вони готові до коміту)
- Робочий каталог чистий

### 1.2. Оновлення main перед створенням нової гілки
```bash
git checkout main
git pull origin main
```
**Важливо:** Завжди оновлюйте `main` перед створенням нової гілки!

### 1.3. Створення нової feature-гілки
```bash
git checkout -b feature/НазваФічі
```
**Приклади назв:**
- `feature/DirrectScreen`
- `feature/UserProfile`
- `feature/ChatFunctionality`

**⚠️ УВАГА:** Якщо плануєте створити гілку `feature/Назва`, спочатку перевірте, чи не існує вже гілка `feature`:
```bash
git branch
```
Якщо гілка `feature` існує, видаліть її або використайте іншу назву (наприклад, `feature-dirrect-screen`).

---

## 💻 КРОК 2: Робота над кодом

### 2.1. Внесення змін
Працюйте над своїм кодом, вносьте зміни в файли.

### 2.2. Перевірка змін
```bash
git status
```
Перевірте, які файли були змінені або додані.

### 2.3. Додавання файлів до staging
```bash
# Додати конкретний файл
git add lib/view/dirrect/dirrect_screen.dart

# Або додати всю директорію
git add lib/view/dirrect/

# Або додати всі зміни
git add .
```

### 2.4. Коміт змін
```bash
git commit -m "feat: опис змін"
```
**Приклади повідомлень комітів:**
- `feat: додано новий екран DirrectScreen`
- `fix: виправлено помилку в авторизації`
- `refactor: оптимізовано код віджетів`

**Формат повідомлення:**
- `feat:` - нова функціональність
- `fix:` - виправлення помилки
- `refactor:` - рефакторинг коду
- `style:` - зміни форматування
- `docs:` - зміни в документації

### 2.5. Відправка гілки на GitHub
```bash
git push -u origin feature/НазваФічі
```
Флаг `-u` встановлює відстеження гілки, щоб надалі можна було використовувати просто `git push`.

---

## 🔄 КРОК 3: Створення Pull Request на GitHub

### 3.1. Перехід на GitHub
Після `git push` GitHub покаже посилання для створення Pull Request:
```
remote: Create a pull request for 'feature/DirrectScreen' on GitHub by visiting:
remote:      https://github.com/username/repo/pull/new/feature/DirrectScreen
```

### 3.2. Створення Pull Request
1. Перейдіть за посиланням або відкрийте репозиторій на GitHub
2. Натисніть кнопку **"Compare & pull request"**
3. Заповніть інформацію:
   - **Title:** Короткий опис змін
   - **Description:** Детальний опис того, що було зроблено
   - **Reviewers:** Виберіть рев'юерів (якщо потрібно)
4. Натисніть **"Create pull request"**

### 3.3. Очікування review (якщо потрібно)
- Чекайте на затвердження від рев'юерів
- Виправте зауваження, якщо вони є
- Після виправлень зробіть новий коміт і push:
  ```bash
  git add .
  git commit -m "fix: виправлено зауваження рев'юера"
  git push
  ```

---

## ✅ КРОК 4: Мердж Pull Request на GitHub

### 4.1. Мердж через GitHub UI
1. Перейдіть на сторінку Pull Request
2. Переконайтеся, що всі перевірки пройшли успішно
3. Натисніть кнопку **"Merge pull request"**
4. Підтвердіть мердж (натисніть **"Confirm merge"**)

### 4.2. Видалення гілки на GitHub (опціонально)
Після мерджу GitHub запропонує видалити гілку. Можна натиснути **"Delete branch"**.

---

## 🔄 КРОК 5: Оновлення локальної гілки main

### 5.1. Перехід на main
```bash
git checkout main
```

### 5.2. Оновлення main з віддаленого репозиторію
```bash
git pull origin main
```
**Це критично важливо!** Ця команда завантажить всі зміни, які були смержені на GitHub.

### 5.3. Перевірка оновлення
```bash
git log --oneline -5
```
Перевірте, що ваш коміт з'явився в історії.

---

## 🧹 КРОК 6: Очищення (опціонально)

### 6.1. Видалення локальної feature-гілки
```bash
git branch -d feature/НазваФічі
```
Якщо Git відмовляється видалити (бо гілка не смержена), використайте:
```bash
git branch -D feature/НазваФічі
```

### 6.2. Видалення віддаленої гілки (якщо не видалена через GitHub UI)
```bash
git push origin --delete feature/НазваФічі
```

---

## 📝 Чеклист перед створенням нової гілки

- [ ] Перевірено `git status` - робочий каталог чистий
- [ ] Перебуваю на гілці `main`
- [ ] Виконано `git pull origin main` для оновлення
- [ ] Перевірено, що назва нової гілки не конфліктує з існуючими

## 📝 Чеклист перед комітом

- [ ] Всі зміни додані через `git add`
- [ ] Перевірено `git status` - всі потрібні файли в staging
- [ ] Написано зрозуміле повідомлення коміту
- [ ] Код протестовано локально

## 📝 Чеклист перед Pull Request

- [ ] Всі зміни закомічені
- [ ] Гілка запушена на GitHub (`git push -u origin feature/Назва`)
- [ ] Код протестовано
- [ ] Немає конфліктів з main

## 📝 Чеклист після мерджу на GitHub

- [ ] Виконано `git checkout main`
- [ ] Виконано `git pull origin main`
- [ ] Перевірено, що зміни з'явилися в main
- [ ] Видалено локальну feature-гілку (якщо потрібно)

---

## ⚠️ Важливі правила

1. **Завжди оновлюйте main перед створенням нової гілки**
2. **Ніколи не комітьте напряму в main** (якщо це не критично)
3. **Завжди робіть `git pull origin main` після мерджу на GitHub**
4. **Використовуйте зрозумілі назви гілок** (`feature/`, `fix/`, `refactor/`)
5. **Пишіть описові повідомлення комітів**

---

## 🆘 Розв'язання проблем

### Проблема: Конфлікт назв гілок
```bash
fatal: cannot lock ref 'refs/heads/feature/DirrectScreen': 'refs/heads/feature' exists
```
**Рішення:** Видаліть конфліктуючу гілку або використайте іншу назву:
```bash
git branch -D feature
git checkout -b feature/DirrectScreen
```

### Проблема: Конфлікти при мерджі
Якщо виникли конфлікти при `git pull origin main`:
1. Вирішіть конфлікти вручну
2. Додайте вирішені файли: `git add .`
3. Завершіть мердж: `git commit`

### Проблема: Забув оновити main перед створенням гілки
```bash
git checkout main
git pull origin main
git checkout -b feature/НазваФічі
```

---

## 📚 Корисні команди

```bash
# Перегляд всіх гілок
git branch -a

# Перегляд історії комітів
git log --oneline --graph --all

# Перегляд змін у файлах
git diff

# Скасування змін у файлі (перед git add)
git restore файл.dart

# Скасування додавання файлу (після git add, перед git commit)
git restore --staged файл.dart
```

